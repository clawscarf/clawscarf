# Generated from services/cloud/openapi.json; do not edit.
openapi: 3.1.0
info:
  title: ClawScarf Connections broker runtime API
  version: 0.0.0
paths:
  /connector-runtime/search:
    post:
      operationId: searchConnectorRuntime
      summary: Connector search
      description:
        Installation credential and trusted native agent context select
        currently permitted exact connections. Human cookies and CLI credentials
        are not accepted. No server identity is accepted in the payload.
      tags:
        - Connector runtime
      security:
        - InstallationRuntime: []
      parameters: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ConnectorRuntimeSearch"
      responses:
        "200":
          description: Current resource.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConnectorRuntimeSearchResult"
        "400":
          $ref: "#/components/responses/InvalidRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "412":
          $ref: "#/components/responses/PreconditionFailed"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/UnsupportedMediaType"
        "429":
          $ref: "#/components/responses/RateLimited"
        "500":
          $ref: "#/components/responses/InternalError"
        "503":
          $ref: "#/components/responses/Unavailable"
  /connector-runtime/describe:
    post:
      operationId: describeConnectorRuntime
      summary: Connector describe
      description:
        Installation credential and trusted native agent context select
        currently permitted exact connections. Human cookies and CLI credentials
        are not accepted. No server identity is accepted in the payload.
      tags:
        - Connector runtime
      security:
        - InstallationRuntime: []
      parameters: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ConnectorRuntimeDescribe"
      responses:
        "200":
          description: Current resource.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConnectorRuntimeDescription"
        "400":
          $ref: "#/components/responses/InvalidRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "412":
          $ref: "#/components/responses/PreconditionFailed"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/UnsupportedMediaType"
        "429":
          $ref: "#/components/responses/RateLimited"
        "500":
          $ref: "#/components/responses/InternalError"
        "503":
          $ref: "#/components/responses/Unavailable"
  /connector-runtime/call:
    post:
      operationId: callConnectorRuntime
      summary: Connector call
      x-fastify-config:
        bodyLimit: 262144
      description:
        Durable invocation identity is scoped to server credential, native
        session and toolCallId. Identical replay returns the existing receipt;
        changed arguments conflict. Never automatically redispatch an uncertain
        call. Exact connection generation must still be current. New dispatch
        also requires the published catalog and current action version;
        authorized receipt replay does not require retired catalog metadata.
      tags:
        - Connector runtime
      security:
        - InstallationRuntime: []
      parameters: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ConnectorRuntimeCall"
      responses:
        "200":
          description: Current resource.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConnectorRuntimeInvocation"
        "400":
          $ref: "#/components/responses/InvalidRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "412":
          $ref: "#/components/responses/PreconditionFailed"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/UnsupportedMediaType"
        "429":
          $ref: "#/components/responses/RateLimited"
        "500":
          $ref: "#/components/responses/InternalError"
        "503":
          $ref: "#/components/responses/Unavailable"
  /connector-runtime/invocations/{invocationId}:
    get:
      operationId: getConnectorRuntimeInvocation
      summary: Recover an invocation result
      description:
        Read an existing result under the current server credential and
        matching agent grant. Never dispatch the operation again. Revoked
        connection generations do not regain access after native state
        restoration.
      tags:
        - Connector runtime
      security:
        - InstallationRuntime: []
      parameters:
        - name: agentId
          in: query
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 160
      responses:
        "200":
          description: Current resource.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConnectorRuntimeInvocation"
        "400":
          $ref: "#/components/responses/InvalidRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "412":
          $ref: "#/components/responses/PreconditionFailed"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/UnsupportedMediaType"
        "429":
          $ref: "#/components/responses/RateLimited"
        "500":
          $ref: "#/components/responses/InternalError"
        "503":
          $ref: "#/components/responses/Unavailable"
    parameters:
      - $ref: "#/components/parameters/ConnectionInvocationId"
  /connector-runtime/invocations/{invocationId}/result:
    get:
      operationId: getConnectorRuntimeResultPage
      summary: Read a bounded page of retained result JSON
      description:
        Read an existing result under the current server credential and
        matching agent grant. Never dispatch the operation again. Revoked
        connection generations do not regain access after native state
        restoration.
      tags:
        - Connector runtime
      security:
        - InstallationRuntime: []
      parameters:
        - name: agentId
          in: query
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 160
        - name: cursor
          in: query
          required: false
          schema:
            type: string
            minLength: 1
            maxLength: 512
      responses:
        "200":
          description: Current resource.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConnectorRuntimeResultPage"
        "400":
          $ref: "#/components/responses/InvalidRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "412":
          $ref: "#/components/responses/PreconditionFailed"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/UnsupportedMediaType"
        "429":
          $ref: "#/components/responses/RateLimited"
        "500":
          $ref: "#/components/responses/InternalError"
        "503":
          $ref: "#/components/responses/Unavailable"
    parameters:
      - $ref: "#/components/parameters/ConnectionInvocationId"
  /connector-runtime/invocations/lookup:
    post:
      operationId: lookupConnectorRuntimeInvocation
      summary: Find a receipt from its original native call identity
      description:
        Read an existing result under the current server credential and
        matching agent grant. Never dispatch the operation again. Revoked
        connection generations do not regain access after native state
        restoration.
      tags:
        - Connector runtime
      security:
        - InstallationRuntime: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ConnectorRuntimeInvocationLookup"
      responses:
        "200":
          description: Current resource.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConnectorRuntimeInvocation"
        "400":
          $ref: "#/components/responses/InvalidRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "412":
          $ref: "#/components/responses/PreconditionFailed"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/UnsupportedMediaType"
        "429":
          $ref: "#/components/responses/RateLimited"
        "500":
          $ref: "#/components/responses/InternalError"
        "503":
          $ref: "#/components/responses/Unavailable"
components:
  securitySchemes:
    InstallationRuntime:
      type: http
      scheme: bearer
      description: Revocable runtime credential scoped to one installation.
  schemas:
    ConnectorRuntimeSearch:
      type: object
      additionalProperties: false
      properties:
        context:
          $ref: "#/components/schemas/ConnectorRuntimeContext"
        query:
          type: string
          maxLength: 500
        connectionId:
          $ref: "#/components/schemas/Id"
        connectorId:
          type: string
          minLength: 1
          maxLength: 100
        limit:
          type: integer
          minimum: 1
          maximum: 25
          default: 10
        cursor:
          type: string
          minLength: 1
          maxLength: 2000
      required:
        - context
    ConnectorRuntimeContext:
      type: object
      additionalProperties: false
      properties:
        agentId:
          type: string
          minLength: 1
          maxLength: 160
        sessionId:
          type: string
          minLength: 1
          maxLength: 500
        sessionKey:
          type: string
          minLength: 1
          maxLength: 1000
        toolCallId:
          type: string
          minLength: 1
          maxLength: 500
      required:
        - agentId
        - toolCallId
      description:
        Populated by the trusted native plugin factory, never by model
        arguments. Calls require sessionId or sessionKey. Installation identity
        comes only from the machine credential.
    Id:
      type: string
      minLength: 1
      description:
        Opaque identifier; clients must not infer identity or provider
        details from it.
    ConnectorRuntimeSearchResult:
      type: object
      additionalProperties: false
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/ConnectorRuntimeMatch"
          maxItems: 25
        nextCursor:
          type:
            - string
            - "null"
          maxLength: 2000
        guidance:
          description:
            Present only when this agent has no usable connections before
            applying search filters. The URL identifies the authenticated
            server; it grants no portal access. Managing connections requires
            the signed-in person's current native administrator permission.
          oneOf:
            - type: "null"
            - $ref: "#/components/schemas/ConnectorRuntimeSearchGuidance"
      required:
        - items
        - nextCursor
        - guidance
    ConnectorRuntimeMatch:
      type: object
      additionalProperties: false
      properties:
        connection:
          type: object
          additionalProperties: false
          properties:
            id:
              $ref: "#/components/schemas/Id"
            name:
              type: string
              minLength: 1
              maxLength: 160
            connectorId:
              type: string
              minLength: 1
              maxLength: 100
            generation:
              type: integer
              minimum: 1
              maximum: 2147483647
          required:
            - id
            - name
            - connectorId
            - generation
        action:
          $ref: "#/components/schemas/ConnectorActionSummary"
      required:
        - connection
        - action
    ConnectorActionSummary:
      type: object
      additionalProperties: false
      properties:
        id:
          type: string
          minLength: 1
          maxLength: 256
        name:
          type: string
          minLength: 1
          maxLength: 300
        description:
          type: string
          maxLength: 500
        connectorId:
          type: string
          minLength: 1
          maxLength: 100
        version:
          type: string
          minLength: 1
          maxLength: 200
        schemaDigest:
          type: string
          minLength: 1
          maxLength: 128
      required:
        - id
        - name
        - description
        - connectorId
        - version
        - schemaDigest
    ConnectorRuntimeSearchGuidance:
      type: object
      additionalProperties: false
      properties:
        code:
          type: string
          enum:
            - no_usable_connections
      required:
        - code
    Problem:
      type: object
      additionalProperties: false
      required:
        - type
        - title
        - status
        - code
        - requestId
      properties:
        type:
          type: string
        title:
          type: string
        status:
          type: integer
        code:
          type: string
        requestId:
          type: string
        quota:
          type: object
          additionalProperties: false
          properties:
            scope:
              type: string
              enum:
                - account
                - installation
                - backend
            unit:
              type: string
              enum:
                - executions
                - requests
                - installations
            limit:
              type: integer
              minimum: 0
            resetsAt:
              type:
                - string
                - "null"
              format: date-time
          required:
            - scope
            - unit
            - limit
            - resetsAt
        detail:
          type: string
        retry:
          $ref: "#/components/schemas/RetryGuidance"
    RetryGuidance:
      description:
        Retry advice does not authorize replaying a mutation. Preserve
        idempotency keys and reconcile uncertain outcomes.
      type: object
      additionalProperties: false
      required:
        - strategy
      properties:
        strategy:
          type: string
          enum:
            - never
            - after_delay
            - after_change
            - reconcile
          description: >
            never means no automatic replay; after_change requires a corrected

            request or changed prerequisite; reconcile means inspect the
            original

            command outcome before deciding whether another attempt is safe.
        afterSeconds:
          type: integer
          minimum: 0
          description:
            Only when a retry time is known; consistent with Retry-After when
            provided.
      examples:
        - strategy: after_change
        - strategy: after_delay
          afterSeconds: 30
        - strategy: reconcile
    ConnectorRuntimeDescribe:
      type: object
      additionalProperties: false
      properties:
        context:
          $ref: "#/components/schemas/ConnectorRuntimeContext"
        connectionId:
          $ref: "#/components/schemas/Id"
        actionId:
          type: string
          minLength: 1
          maxLength: 256
      required:
        - context
        - connectionId
        - actionId
    ConnectorRuntimeDescription:
      type: object
      additionalProperties: false
      properties:
        connectionId:
          $ref: "#/components/schemas/Id"
        generation:
          type: integer
          minimum: 1
          maximum: 2147483647
        action:
          $ref: "#/components/schemas/ConnectorActionDetail"
      required:
        - connectionId
        - generation
        - action
    ConnectorActionDetail:
      type: object
      additionalProperties: false
      properties:
        id:
          type: string
          minLength: 1
          maxLength: 256
        name:
          type: string
          minLength: 1
          maxLength: 300
        description:
          type: string
          maxLength: 65536
        connectorId:
          type: string
          minLength: 1
          maxLength: 100
        version:
          type: string
          minLength: 1
          maxLength: 200
        schemaDigest:
          type: string
          minLength: 1
          maxLength: 128
        inputSchema:
          description:
            Opaque provider schema hint or JSON data. Never use it as a
            transport validator.
        outputSchema:
          description:
            Opaque provider schema hint or JSON data. Never use it as a
            transport validator.
        fileInputs:
          type: array
          items:
            type: string
            minLength: 1
            maxLength: 1000
          maxItems: 1000
        fileOutputs:
          type: array
          items:
            type: string
            minLength: 1
            maxLength: 1000
          maxItems: 1000
      required:
        - id
        - name
        - description
        - connectorId
        - version
        - schemaDigest
        - inputSchema
        - outputSchema
        - fileInputs
        - fileOutputs
    ConnectorRuntimeCall:
      type: object
      additionalProperties: false
      properties:
        context:
          $ref: "#/components/schemas/ConnectorRuntimeContext"
        connectionId:
          $ref: "#/components/schemas/Id"
        generation:
          type: integer
          minimum: 1
          maximum: 2147483647
        actionId:
          type: string
          minLength: 1
          maxLength: 256
        version:
          type: string
          minLength: 1
          maxLength: 200
        arguments:
          type: object
          additionalProperties: true
          description:
            Generic JSON object. Provider input schemas are advisory and are
            not enforced or repaired. Serialized arguments are limited to 128
            KiB of UTF-8 JSON. The complete HTTP request body is limited to 256
            KiB.
      required:
        - context
        - connectionId
        - generation
        - actionId
        - version
        - arguments
      description:
        Durable invocation identity is scoped to server credential, native
        session and toolCallId. Identical replay returns the existing receipt;
        changed arguments conflict. Never automatically redispatch an uncertain
        call. Exact connection generation and catalog action version must still
        be current.
    ConnectorRuntimeInvocation:
      type: object
      additionalProperties: false
      properties:
        invocation:
          $ref: "#/components/schemas/ConnectionInvocation"
        result:
          oneOf:
            - type: object
              additionalProperties: false
              properties:
                kind:
                  const: inline
                  type: string
                data: {}
              required:
                - kind
                - data
            - $ref: "#/components/schemas/ConnectorResultReference"
            - $ref: "#/components/schemas/ConnectorResultUnavailable"
      required:
        - invocation
        - result
      description:
        Execution state and idempotency survive result expiry. Results are
        retained for 24 hours after completion. JSON above the 32 KiB inline
        limit is read through bounded result pages. Expired or unavailable data
        never changes a succeeded execution to failure or permits a new
        dispatch.
    ConnectionInvocation:
      type: object
      additionalProperties: false
      properties:
        id:
          $ref: "#/components/schemas/Id"
        connectionId:
          $ref: "#/components/schemas/Id"
        generation:
          type: integer
          minimum: 1
          maximum: 2147483647
        agentId:
          type: string
          minLength: 1
          maxLength: 160
        actionId:
          type: string
          minLength: 1
          maxLength: 256
        version:
          type: string
          minLength: 1
          maxLength: 200
        state:
          type: string
          enum:
            - dispatching
            - succeeded
            - rejected
            - outcome_unknown
        createdAt:
          type: string
          format: date-time
        completedAt:
          type:
            - string
            - "null"
          format: date-time
        failure:
          oneOf:
            - $ref: "#/components/schemas/ConnectionFailure"
            - type: "null"
      required:
        - id
        - connectionId
        - generation
        - agentId
        - actionId
        - version
        - state
        - createdAt
        - completedAt
        - failure
    ConnectionFailure:
      type: object
      additionalProperties: false
      properties:
        code:
          type: string
          enum:
            - provider_unavailable
            - provider_rejected
            - provider_not_configured
            - setup_expired
            - setup_cancelled
            - setup_outcome_unknown
            - account_unavailable
            - account_mismatch
            - credential_revoked
            - catalog_changed
            - outcome_unknown
            - result_unavailable
        detail:
          type: string
          minLength: 1
          maxLength: 1000
        retry:
          $ref: "#/components/schemas/RetryGuidance"
      required:
        - code
        - detail
        - retry
    ConnectorResultReference:
      type: object
      additionalProperties: false
      properties:
        kind:
          type: string
          const: reference
        invocationId:
          $ref: "#/components/schemas/Id"
        mediaType:
          type: string
          const: application/json
        byteLength:
          type: integer
          minimum: 1
          maximum: 8388608
        sha256:
          type: string
          pattern: ^[a-f0-9]{64}$
        expiresAt:
          type: string
          format: date-time
      required:
        - kind
        - invocationId
        - mediaType
        - byteLength
        - sha256
        - expiresAt
    ConnectorResultUnavailable:
      type: object
      additionalProperties: false
      properties:
        kind:
          type: string
          const: unavailable
        reason:
          type: string
          enum:
            - pending
            - failed
            - outcome_unknown
            - too_large
            - invalid_result
            - expired
      required:
        - kind
        - reason
    ConnectorRuntimeResultPage:
      type: object
      additionalProperties: false
      properties:
        invocation:
          $ref: "#/components/schemas/ConnectionInvocation"
        result:
          oneOf:
            - $ref: "#/components/schemas/ConnectorResultPage"
            - $ref: "#/components/schemas/ConnectorResultUnavailable"
      required:
        - invocation
        - result
    ConnectorResultPage:
      type: object
      additionalProperties: false
      description:
        Exact UTF-8 JSON text, at most 8192 UTF-8 bytes per page. Join page
        text in byte-offset order and verify the reference byte length and
        SHA-256 to recover the complete serialized result. No page is an
        independent JSON document. Cursor is a selector, not authorization;
        every page rechecks current access.
      properties:
        kind:
          type: string
          const: page
        reference:
          $ref: "#/components/schemas/ConnectorResultReference"
        text:
          type: string
          maxLength: 8192
        offsetBytes:
          type: integer
          minimum: 0
          maximum: 8388608
        nextCursor:
          type:
            - string
            - "null"
          maxLength: 512
      required:
        - kind
        - reference
        - text
        - offsetBytes
        - nextCursor
    ConnectorRuntimeInvocationLookup:
      type: object
      additionalProperties: false
      description: Read only. Find the receipt for the original tool call in the
        current native agent/session. Missing receipts return 404 and never
        allocate or execute an invocation. Native context is supplied by the
        plugin, not model parameters.
      properties:
        context:
          $ref: "#/components/schemas/ConnectorRuntimeContext"
        targetToolCallId:
          type: string
          minLength: 1
          maxLength: 500
      required:
        - context
        - targetToolCallId
  responses:
    InvalidRequest:
      description: Invalid JSON, field or request shape; code invalid_request.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/Problem"
    Unauthorized:
      description: Missing or invalid session or runtime credential; code unauthenticated.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/Problem"
    Forbidden:
      description:
        Authenticated caller lacks the required current authority; code
        forbidden.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/Problem"
    NotFound:
      description: Resource absent or not accessible in the server; code not_found.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/Problem"
    Conflict:
      description:
        The command conflicts with current state or a previous idempotent
        request.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/Problem"
    PreconditionFailed:
      description: The observed resource revision is stale; code revision_conflict.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/Problem"
    PayloadTooLarge:
      description: Request exceeds this route's configured body limit; code
        request_too_large.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/Problem"
    UnsupportedMediaType:
      description: Request body must use application/json; code unsupported_media_type.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/Problem"
    RateLimited:
      description: Request limit reached; code rate_limited.
      headers:
        Retry-After:
          schema:
            type: integer
            minimum: 0
          description: Delay in seconds, if known.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/Problem"
    InternalError:
      description: Safe failure response; no provider payload, stack trace or
        credential is exposed.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/Problem"
    Unavailable:
      description: Required service unavailable; code dependency_unavailable.
      headers:
        Retry-After:
          schema:
            type: integer
            minimum: 0
          description: Delay in seconds, if known.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/Problem"
  parameters:
    ConnectionInvocationId:
      name: invocationId
      in: path
      required: true
      schema:
        $ref: "#/components/schemas/Id"
